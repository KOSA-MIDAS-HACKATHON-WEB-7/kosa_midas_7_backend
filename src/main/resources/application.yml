# database
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/midas-hackathon
    username: root
    password: ${PASSWORD}
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update